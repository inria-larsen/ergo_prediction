import numpy as np
import matplotlib.pyplot as plt
import pickle
import csv
import pandas as pd
import seaborn as sns
import random 
import pickle


def sync_labels(timestamps, labels):
	labels_sync = []

	print(np.shape(timestamps), np.shape(labels))

	# index = 0
	# for t in t_labels:
	# 	if t > timestamps[index]:
	# 		labels_sync.append(labels[index])
	# 		index += 1

	return labels_sync



